import 'package:flutter/material.dart';

class UserDetailFormWidget extends StatelessWidget {
  const UserDetailFormWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}